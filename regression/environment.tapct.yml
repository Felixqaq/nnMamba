name: tapct
channels:
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
      - --index-url
      - https://download.pytorch.org/whl/cu128
      - torch==2.8.0
      - torchvision==0.23.0
      - --extra-index-url
      - https://pypi.org/simple
      - transformers>=4.50
      - huggingface_hub
      - safetensors
      - SimpleITK>=2.5
      - monai>=1.4
      - scikit-learn
      - pandas
      - matplotlib
      - tqdm
      - pyyaml
      - nibabel
      - timm
      - einops

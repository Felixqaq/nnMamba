# 研究機備份清單（明天到學校抓）

研究機路徑前綴：`/home/felix/Research/nnMamba/`
建議帶 **64 GB 以上**的硬碟，估計 20–30 GB。

---

## 必抓（沒有就無法重訓）

```
classification/datasets/normal_v_abnormal_66/     ← 66 例 NIfTI，最重要
figures/                                          ← 所有 run 的 results.json + 圖
origin_dataset/                                   ← 原始 DICOM（至少 A267542）
```

一行指令打包：

```bash
cd /home/felix/Research/nnMamba
tar czf /media/USB/nnmamba_backup.tar.gz \
    classification/datasets/normal_v_abnormal_66/ \
    figures/ \
    origin_dataset/ \
    *.json
```

## 要找出來（現有權重的來源）

`models/current/metrics.json` 記載發布權重是 **2026-07-23、100 epochs、seeds 42–46**。
但本機留存的 run log 是 **2026-02/03、50 epochs、batch 12、lr 1e-4、5-fold seed 42**——
**不是同一次訓練**。所以要找：

- [ ] 7 月那次訓練的 config / 指令 / log（`figures/` 或 `results/` 底下，日期 2026-07-23 前後）
- [ ] `0.726 / 0.803` 那組 nested-CV 的原始 `results.json`
      （metrics.json 稱其為 "nested CV, 66-case, RQ1 image-only"，論文要引用就必須有原始紀錄）

抓不到也沒關係，但那樣就等於**現有權重無法復現**，重訓只能重新定義一組超參數。

## 順手帶（有就好）

```bash
conda env export -n <你的環境名> > /media/USB/env_research.yml
history > /media/USB/bash_history.txt      # 找得回當初的指令
```

---

## 帶回來之後

我這邊已經確認：
- 這台 RTX 3060 Ti 訓練綽綽有餘（batch 8 只用 1.77 GB / 8.6 GB，全套 5-seed 約 20 分鐘）
- Docker image `felix48529319/copd-ct-app:latest` 內已有編好的 mamba-ssm，直接當訓練環境用
- app 的預處理與 nnMamba `regression/data/dataset.py` **逐行一致**，超參數也相同，沒有被刪減
- 外部驗證集已備妥：`20260811dicom` 27 例，與訓練 66 例**零重疊**，每例都有 PFT ground truth

**重訓時務必保留 held-out 測試集**——現有權重 `held_out: false`，這是它拿不出任何可信效能的根本原因。
